# Auto-Fill Half Stack on Split

When you right-click a stacked item in your inventory and pick "Split", the "Remove how many from stack?" dialog opens. With this feature on, that dialog comes up pre-filled with half the stack size (rounded up) instead of 1, so you can confirm a roughly even split in one click.
