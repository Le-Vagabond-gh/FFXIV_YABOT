# Commendation Tracker

When the end-of-duty commendation popup appears, shows your live commendation count (and `+N` for ones gained this run) so you can see in real time whether other players actually commend you. Includes a brief golden glow + sparkle animation each time a commendation lands. Debug command: `/ycommend [reset]`.
